# multipay-frontend-ts (frontend checkout/picker library) — placeholder.
# Implements the dispatcher target set as no-ops until the port is built,
# so the root dispatcher Makefile runs cleanly across all language folders.

.PHONY: all build lint test check format clean help

all: build

build:
	@echo "multipay-frontend-ts not yet implemented — skipping build"

lint:
	@echo "multipay-frontend-ts not yet implemented — skipping lint"

test:
	@echo "multipay-frontend-ts not yet implemented — skipping test"

check:
	@echo "multipay-frontend-ts not yet implemented — skipping check"

format:
	@echo "multipay-frontend-ts not yet implemented — skipping format"

clean:
	@echo "multipay-frontend-ts not yet implemented — skipping clean"

help:
	@echo "multipay-frontend-ts (placeholder) — targets: build lint test check format clean"
