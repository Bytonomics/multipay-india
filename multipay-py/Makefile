# multipay-py (backend Python port) — placeholder.
# Implements the dispatcher target set as no-ops until the port is built,
# so the root dispatcher Makefile runs cleanly across all language folders.

.PHONY: all build lint test check format clean help

all: build

build:
	@echo "multipay-py not yet implemented — skipping build"

lint:
	@echo "multipay-py not yet implemented — skipping lint"

test:
	@echo "multipay-py not yet implemented — skipping test"

check:
	@echo "multipay-py not yet implemented — skipping check"

format:
	@echo "multipay-py not yet implemented — skipping format"

clean:
	@echo "multipay-py not yet implemented — skipping clean"

help:
	@echo "multipay-py (placeholder) — targets: build lint test check format clean"
